[project]
name = "llm-service"
version = "0.1.0"
description = "LLM orchestration service (Ollama) for the voice assistant pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.140.9",
    "uvicorn[standard]>=0.30",
    "uvicorn>=0.51.0",
    "ollama>=0.6.2",
]

